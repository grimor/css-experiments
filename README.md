# CSS Math functions demos

This repository contains an demos code for JSGuild 2024 lighting talk "CSS Math Function".

## Run

1. `npm install / yarn install / pnpm install`
2. `npm run dev / yarn dev / pnpm dev`
3. Open browser on `localhost:4321`
